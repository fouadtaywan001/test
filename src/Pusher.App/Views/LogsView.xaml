<UserControl x:Class="Pusher.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <!-- Header -->
        <Grid DockPanel.Dock="Top" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Logs" Style="{StaticResource PageTitle}" />
                <TextBlock Text="Background worker activity — rolling daily files"
                           Style="{StaticResource Subtle}" Margin="0,2,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Refresh" Style="{StaticResource GhostButton}"
                        Command="{Binding RefreshCommand}" />
                <Button Content="Open folder" Style="{StaticResource GhostButton}"
                        Command="{Binding OpenFolderCommand}" Margin="8,0,0,0" />
            </StackPanel>
        </Grid>

        <!-- Filter bar -->
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" Width="200"
                          ItemsSource="{Binding LogFiles}"
                          SelectedItem="{Binding SelectedFile}" />
                <ComboBox Grid.Column="1" Width="90" Margin="8,0,0,0"
                          ItemsSource="{Binding LevelFilters}"
                          SelectedItem="{Binding LevelFilter}" />
                <TextBox Grid.Column="2" Margin="8,0,0,0"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         ToolTip="Filter log lines (case-insensitive)" />
            </Grid>
        </Border>

        <!-- Status line -->
        <TextBlock DockPanel.Dock="Top" Text="{Binding StatusMessage}"
                   Style="{StaticResource Subtle}" Margin="2,0,0,10" TextWrapping="Wrap" />

        <!-- Log lines -->
        <Border Style="{StaticResource Card}" Padding="0"
                Visibility="{Binding HasFiles, Converter={StaticResource BoolToVisibility}}">
            <ListView x:Name="LinesList" ItemsSource="{Binding Lines}"
                      Background="Transparent" BorderThickness="0"
                      Foreground="{StaticResource Text}"
                      MaxHeight="520"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="12,2" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="34" Margin="0,1,8,1"
                                    Background="Transparent" CornerRadius="4">
                                <TextBlock Text="{Binding Level}" Foreground="{Binding LevelColor}"
                                           FontFamily="{StaticResource MonoFont}" FontSize="11"
                                           FontWeight="Bold" HorizontalAlignment="Center" />
                            </Border>
                            <TextBlock Grid.Column="1" Text="{Binding Text}"
                                       Foreground="{StaticResource Text}"
                                       FontFamily="{StaticResource MonoFont}" FontSize="12"
                                       TextWrapping="NoWrap" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </DockPanel>
</UserControl>
